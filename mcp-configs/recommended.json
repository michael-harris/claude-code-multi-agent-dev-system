{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Recommended MCP servers for enhanced DevTeam functionality",

  "mcpServers": {
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "metadata": {
        "purpose": "Enhanced reasoning for complex planning and architecture decisions",
        "benefits": "Improves quality of architectural decisions and complex bug analysis",
        "context_cost": "~5k tokens"
      }
    },

    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "${PROJECT_ROOT}"],
      "metadata": {
        "purpose": "Enhanced file operations",
        "benefits": "More efficient file handling for large codebases",
        "context_cost": "~3k tokens"
      }
    }
  },

  "installation": {
    "instructions": [
      "1. Copy desired servers to your MCP configuration",
      "2. Replace ${PROJECT_ROOT} with your actual project path",
      "3. These are optional but improve DevTeam performance"
    ]
  }
}
